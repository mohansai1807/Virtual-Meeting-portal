# Zoom
A full stack video conferencing web application.
## Configuration

Copy `backend/.env.example` to `backend/.env` and set `MONGO_URI`. For the
frontend, copy `frontend/.env.example` to `frontend/.env` and set
`REACT_APP_SERVER_URL` to the backend URL. Restart the frontend after changing
its environment variables.
